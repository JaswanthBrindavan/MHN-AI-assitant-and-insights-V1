"""Vision — reading images the reader is entitled to.

Gated by the same four-condition consent gate as every other family read (see
app/documents/fetch.py), and its output is treated as untrusted generated text.
"""
